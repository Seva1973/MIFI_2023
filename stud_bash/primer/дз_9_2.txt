Задание состоит из трёх этапов:

Напишите Bash-скрипт для резервного копирования директории /home, конфигурационных файлов основных утилит удалённого доступа (SSH, RDP, FTP), а также директории /var/log.
В cron создайте задачу для запуска этого скрипта каждую пятницу в 18:30.
Переместите получившийся бэкап в отдельную заранее созданную директорию /archive. 
Конечное решение скрипта может выглядеть следующим образом:

bash
Copy code
#!/bin/bash

# Создаем переменные для хранения путей к директориям
home_dir="/home"
remote_access_dir="/etc"
log_dir="/var/log"
backup_dir="/archive"

# Создаем архив с помощью tar
tar -czf backup.tar.gz $home_dir $remote_access_dir $log_dir

# Перемещаем архив в директорию /archive
mv backup.tar.gz $backup_dir
Создание задачи в cron для запуска скрипта каждую пятницу в 18:30:

bash
Copy code
# Открываем crontab для редактирования
crontab -e

# Добавляем следующую строку в конец файла
30 18 * * 5 /path/to/backup_script.sh
В этой строке мы указываем, что скрипт должен запускаться каждую пятницу (5) в 18:30 (30 18). Убедитесь, что вы заменили /path/to/backup_script.sh на фактический путь к вашему скрипту.

Чтобы переместить полученный бэкап в отдельную директорию /archive, можно добавить соответствующую строку в конец скрипта:

bash
Copy code
mv backup.tar.gz $backup_dir
Эта строка переместит архив в директорию /archive, которая уже должна быть создана. Убедитесь, что вы заменили $backup_dir на фактический путь к вашей директории /archive.





Regenerate response

